<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>User Management</title>
</head>
<body>
    
    
    <div class="container col-md-6">
        <!-- <h3>Products DashBoard</h3> -->
        
    <table class="table" style="margin-top: 10%;">  
        <thead class="bg-dark text-white">
          <tr>
            <th scope="col">id</th>
            <th scope="col"> Username</th>
            <th scope="col">Email</th>
            <th scope="col">Role</th>

            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
         
            <% for(user of users){ %> 
          <tr>
            <th scope="row"><%= user.id %> </th>
            <td><%= user.username %> </td>
            <td><%= user.email %> </td>
            <td><%= user.Role %> </td>
            <td class="ml-4"><div class="btn-group">
                <a href="users/<%= user.id %>/edit" class="btn btn-outline-primary btn-sm">Edit</a> <form action="/users/<%= user.id %>?_method=DELETE" method="post"> <button type="submit" class="btn btn-outline-danger btn-sm">Remove</button></form> 
            </div>
        </td>
          </tr>
          <% } %> 
        </tbody>
      </table>
      <a href="/users/new" style="text-decoration: none; border: none; background-color: rgb(8, 158, 8); padding: 10px; color: white; font-weight: 600; border-radius: 5px;">Add user</a>
      <% if(user.length<=0){ %> 
        <div class="mt-4">
        <p>No User in the system</p>
    </div>
        <% } %>
    </div>

    <style>
        body{
            background-color: rgb(186, 252, 230);
        }
    </style>
</body>
</html>